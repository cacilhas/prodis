%	host to bind
setting(prodis:host, localhost).

%	port to bind
setting(prodis:port, 5000).

%	cache file
setting(prodis:cache, '/tmp/cache').

%	log file
setting(prodis:logfile, '/dev/null').

%	log level
setting(prodis:loglevel, error).

%   password
setting(prodis:password, ["testpass"]).
