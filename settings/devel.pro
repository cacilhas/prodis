%	host to bind
setting(prodis:host, localhost).

%	port to bind
setting(prodis:port, 6379).

%	cache file
setting(prodis:cache, 'cache.db').

%	log file
setting(prodis:logfile, user).

%	log level
setting(prodis:loglevel, debug).

%   password
setting(prodis:password, ["guest"]).
